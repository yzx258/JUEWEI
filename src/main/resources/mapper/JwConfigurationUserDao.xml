<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rbgt.jw.dao.JwConfigurationUserDao">

    <!-- 分页搜索 -->
    <select id="search" resultType="com.rbgt.jw.base.dto.JwConfigurationUserDTO">
        select jcu.* from jw_configuration_user jcu
<!--        <where>-->
<!--            &lt;!&ndash; 关键字搜索[模板，需要修改] &ndash;&gt;-->
<!--            <if  test="param.keyword != null and param.keyword != ''">-->
<!--                AND (jcu.name LIKE concat('%', #{param.keyword} , '%') OR jcu.no LIKE concat('%', #{param.keyword} , '%'))-->
<!--            </if>-->
<!--        </where>-->
    </select>
</mapper>